# This file makes 'models' a Python sub-package of 'app'.
# It can be empty.
# This allows imports like 'from .models_product_module import ...' from within this package,
# or 'from app.models.models_product_module import ...' from outside (if 'app' is in PYTHONPATH).
