# This file makes 'app' a Python package
# It can be empty, or can contain package-level initializations if needed later.
# For now, keeping it simple.
# This also helps with relative imports within the 'app' package.
